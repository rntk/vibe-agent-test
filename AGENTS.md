## Type Annotations

**All code written by agents must include proper type annotations/hints.**

### Python

- Use type hints for function parameters and return types
- Use `typing` module for complex types (List, Dict, Optional, Union, etc.)